version: "3"
services:
  web:
    build: ./Dockerfile
    ports:
      - "8000:80"
    depends_on:
      - db
  db:
    image: "mcr.microsoft.com/mssql/server:2017-latest-ubuntu"
    environment:
      SA_PASSWORD: "mypass"
      ACCEPT_EULA: "Y"
